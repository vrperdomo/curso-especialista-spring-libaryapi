spring:
  application:
    name: libaryapi
  datasource:
    url: jdbc:postgresql://localhost:5432/libary
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate.hibernate.format_sql: true
